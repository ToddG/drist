#!/bin/sh

#: configure
# This simple script will set options in the makefile.

prefix='$\(HOME\)'
mandir='$\(HOME\)\/man\/man1'
for arg in "$@"; do
    case "$arg" in
    --mandir=*)
        mandir=`echo $arg | sed 's/--mandir=//'`
        ;;
    --prefix=*)
        prefix=`echo $arg | sed 's/--prefix=//'`
        ;;
    --help)
        echo 'usage: ./configure [options]'
        echo 'options:'
        echo '  --prefix=<path>: installation prefix (default $(HOME)/bin)'
        echo '  --mandir=<path>: installation prefix (default $(HOME)/man/man1)'
        echo 'all invalid options are silently ignored'
        exit 0
        ;;
    esac
done
sed -i '' "s/^PREFIX.*/PREFIX	        :=	$prefix/g" makefile
sed -i '' "s/^MANDIR.*/MANDIR	        :=	$mandir/g" makefile
echo 'configuration complete, type `make` to build.'
